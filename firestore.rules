rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // التطبيق يستخدم نظام تسجيل دخول محلي مخصص، لذا يجب السماح بالقراءة والكتابة
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
